NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only. Random, at least 32 bytes (e.g. `openssl rand -base64 48`). Signs the invite-purpose
# cookie; without it invite links fail closed. Never prefix with NEXT_PUBLIC_.
ADMIN_INVITE_BINDING_SECRET=
